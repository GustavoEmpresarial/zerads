services:
  zerads-callback:
    build:
      context: .
    container_name: zerads-callback
    ports:
      - "${HOST_PORT:-5001}:5001"
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5001/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s